import { AdminEmptyState } from '@/components/admin/AdminStates';

export default function MediaPage() {
  return (
    <AdminEmptyState
      title="Media/File Management"
      description="media_assets entity is provisioned for upload metadata, ownership, and tagging."
    />
  );
}