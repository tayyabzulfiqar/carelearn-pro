import { AdminEmptyState } from '@/components/admin/AdminStates';

export default function CertificatesAdminPage() {
  return (
    <AdminEmptyState
      title="Certificate System Foundation"
      description="Entity structure, issue rules, and verification APIs are connected. UI issuance workflow can be expanded next."
    />
  );
}