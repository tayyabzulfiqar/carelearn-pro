import { AdminEmptyState } from '@/components/admin/AdminStates';

export default function SettingsPage() {
  return (
    <AdminEmptyState
      title="Settings System Foundation"
      description="Tenant-level settings persistence is enabled with organization_settings table."
    />
  );
}