export const metadata = {
  title: 'CareLearn Pro Dashboard',
  description: 'Training dashboard for care home learning and compliance.',
};

export default function DashboardLayout({ children }) {
  return children;
}
